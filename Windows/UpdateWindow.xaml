<Window x:Class="GazeStream.Windows.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Actualizando GazeStream"
        Height="220"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="True"
        >

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="GazeStream se está actualizando..."
                   FontSize="18"
                   FontWeight="Bold"/>

        <StackPanel Grid.Row="1"
                    Margin="0,10,0,0">

            <TextBlock Text="{Binding CurrentVersion,
                StringFormat=Current Version: {0}}"/>

            <TextBlock Text="{Binding NewVersion,
                StringFormat=New Version: {0}}"/>

        </StackPanel>

        <TextBlock Grid.Row="3"
                   Margin="0,10,0,5"
                   Text="{Binding Status}" />

        <ProgressBar Grid.Row="4"
                     Height="22"
                     Minimum="0"
                     Maximum="100"
                     Value="{Binding Progress}" />

        <TextBlock Grid.Row="5"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Text="{Binding Progress,
                       StringFormat={}{0}%}" />

        <!--<Button Grid.Row="6"
                Width="100"
                Height="30"
                Margin="0,10,0,0"
                HorizontalAlignment="Center"
                Content="Cancelar"
                Click="Cancel_Click"/>-->

    </Grid>
</Window>