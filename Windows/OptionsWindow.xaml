<Window x:Class="GazeStream.Windows.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GazeStream.Windows"
        mc:Ignorable="d"
        Title="Opciones" Height="800" Width="500" Background="Gray">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20">
            <TextBlock Text="Kalman Filter" Margin="0,0,0,4"/>
            <Slider Minimum="0"
            Maximum="100"
            TickFrequency="1"
            IsSnapToTickEnabled="True"
            Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="20">
            <TextBlock Text="Another Filter with no name" Margin="0,0,0,4"/>
            <Slider Minimum="0"
     Maximum="100"
     TickFrequency="1"
     IsSnapToTickEnabled="True"
     Width="200"/>
        </StackPanel>
    </Grid>
</Window>
