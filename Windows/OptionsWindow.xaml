<Window x:Class="GazeStream.Windows.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GazeStream.Windows"
        xmlns:pages="clr-namespace:GazeStream.Pages"
        mc:Ignorable="d"
        WindowState="Maximized"
        ShowInTaskbar="True"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Title="Opciones" Height="1080" Width="1920" Background="Gray">
    <Grid>

        <TabControl x:Name="tabControl" Style="{StaticResource SettingsTabControlStyle}" SelectionChanged="OnTabChanged">

            <TabItem Header="Interacción">
                <pages:InteractionSettingsPage/>
            </TabItem>

            <TabItem Header="Cursor">
                <pages:CursorSettingsPage/>
            </TabItem>
            
            <TabItem Header="Placeholder">
                <!--<local:ProfilesPage/>-->
            </TabItem>
        </TabControl>

    </Grid>
</Window>
