<Window x:Class="GazeStream.Windows.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GazeStream.Windows"
        xmlns:pages="clr-namespace:GazeStream.Pages"
        xmlns:control="clr-namespace:GazeStream.Controls"
        xmlns:eye="clr-namespace:GazeStream.Eyetracker"
        xmlns:controls="clr-namespace:GazeStream.Controls"
        xmlns:settings="clr-namespace:GazeStream.AppData"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=settings:Settings}"
        WindowState="Maximized"
        ShowInTaskbar="True"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Title="Opciones" Height="1080" Width="1920" Background="{StaticResource OptionsBackground}">

    <Window.Resources>
        <Style x:Key="TabTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="GazeTab" TargetType="control:GazeTabItem">
            <Setter Property="Width" Value="200"/>
            <!--<Setter Property="HorizontalAlignment" Value="Stretch"/>-->
        </Style>

        <Style x:Key="IconStyle" TargetType="Image">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource OptionsBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="DimGray">
                <eye:GazeButton Width="500" HorizontalAlignment="Left" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" IgnoreRestMode="True">
                    <eye:GazeButton.Content>
                        <controls:IconToggleButton Content="Descanso" IconSize="35" IconOn="/Resources/Icons/eyeOff.png" IconOff="/Resources/Icons/eyeOn.png" IsChecked="{Binding RestModeToggle.Value}"/>
                    </eye:GazeButton.Content>
                </eye:GazeButton>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Grid Width="400">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="GazeDevice" Grid.Row="0" HorizontalAlignment="Right" Text="Dispositivo: Joaco" FontWeight="SemiBold" Foreground="White" FontSize="24" Margin="10,10,50,20"/>
                    <Button x:Name="UpdateButton" Grid.Row="1" Click="Update_App_Click" Width="300" HorizontalAlignment="Right" VerticalAlignment="Stretch" Background="White" Margin="2">
                        <TextBlock x:Name="UpdateText" Text="Update" Foreground="Black" FontWeight="Bold" TextWrapping="Wrap" TextAlignment="Center"/>
                    </Button>
                </Grid>
                
                <eye:GazeButton Width="200" VerticalAlignment="Stretch" HorizontalAlignment="Right">
                <eye:GazeButton.Content>
                    <Button x:Name="CloseButton" Background="Red" Click="Close_Click">
                        <TextBlock Text="X" Foreground="White" FontWeight="Bold" TextWrapping="Wrap" TextAlignment="Center"/>
                    </Button>
                </eye:GazeButton.Content>
            </eye:GazeButton>
            </StackPanel>
        </Grid>
        <TabControl x:Name="MainTabControl" Grid.Row="1" SelectionChanged="OnTabChanged" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{StaticResource OptionsBackground}">
            <TabControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1" IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </TabControl.ItemsPanel>

            <control:GazeTabItem x:Name="Interaction" Style="{StaticResource GazeTab}">
                <control:GazeTabItem.GazeContent>
                    <Grid>
                        <TextBlock Text="Interacción" Style="{StaticResource TabTitle}"/>
                    </Grid>
                </control:GazeTabItem.GazeContent>
                <pages:InteractionSettingsPage/>
            </control:GazeTabItem>

            <control:GazeTabItem x:Name="Cursor" Style="{StaticResource GazeTab}">
                <control:GazeTabItem.GazeContent>
                    <TextBlock Text="Cursor" Style="{StaticResource TabTitle}"/>
                </control:GazeTabItem.GazeContent>
                <pages:CursorSettingsPage/>
            </control:GazeTabItem>

            <control:GazeTabItem x:Name="Keyboard" Style="{StaticResource GazeTab}">
                <control:GazeTabItem.GazeContent>
                    <TextBlock Text="Teclado" Style="{StaticResource TabTitle}"/>
                </control:GazeTabItem.GazeContent>
                <pages:KeyboardSettingsPage/>
            </control:GazeTabItem>

            <control:GazeTabItem x:Name="Barrido" Style="{StaticResource GazeTab}">
                <control:GazeTabItem.GazeContent>
                    <TextBlock Text="Barrido" Style="{StaticResource TabTitle}"/>
                </control:GazeTabItem.GazeContent>
                <pages:BarridoSettingsPage/>
            </control:GazeTabItem>

            <control:GazeTabItem x:Name="Sound" Style="{StaticResource GazeTab}">
                <control:GazeTabItem.GazeContent>
                    <TextBlock Text="Sonido" Style="{StaticResource TabTitle}"/>
                </control:GazeTabItem.GazeContent>
                <pages:SoundSettingsPage/>
            </control:GazeTabItem>

            <control:GazeTabItem x:Name="Help" Style="{StaticResource GazeTab}">
                <control:GazeTabItem.GazeContent>
                    <TextBlock Text="Ayuda" Style="{StaticResource TabTitle}"/>
                </control:GazeTabItem.GazeContent>
                <pages:AyudaSettingsPage/>
            </control:GazeTabItem>
        </TabControl>

    </Grid>
</Window>
