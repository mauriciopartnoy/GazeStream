<UserControl x:Class="GazeStream.Pages.CursorSettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:GazeStream.Controls"
  xmlns:settings="clr-namespace:GazeStream.AppData"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
 mc:Ignorable="d"
 d:DataContext="{d:DesignInstance Type=settings:Settings}"
 d:DesignHeight="1080" d:DesignWidth="1920">

    <Grid Margin="24" Background="{StaticResource OptionsBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT PANEL: SETTINGS -->
        <ScrollViewer x:Name="Scroll">
            <StackPanel Grid.Column="0" VerticalAlignment="Top">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Margin" Value="0,10,0,0"/>
                    </Style>
                    <Style TargetType="{x:Type StackPanel}">
                        <Setter Property="Margin" Value="10,10,0,0"/>
                    </Style>
                </StackPanel.Resources>

                <TextBlock Text="Estilo de puntero" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <controls:EnumSelectorUniform SelectedValue="{Binding CursorStyle.Value}" Height="200"/>

                <TextBlock Text="Tamaño de burbuja" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <controls:EnumSelectorUniform SelectedValue="{Binding BubbleSizeOption.Value}" Height="200"/>

                <TextBlock Text="Color de burbuja" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <controls:EnumSelectorUniform SelectedValue="{Binding BubbleColor.Value}" Height="200"/>

                <!--<Button Width="200" Height="200" Content="SetCursor" Click="OpenCursorDialog"/>-->
            </StackPanel>
        </ScrollViewer>


        <!-- RIGHT PANEL: PREVIEW -->
        <Border Grid.Column="1"
                Margin="24,0,0,0"
                Background="{StaticResource OptionsBackground}"
                CornerRadius="12"
                Padding="24">

            <Grid>
                <TextBlock Text="Preview"
                           Foreground="#888"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Top"/>
                <controls:GazeCursor HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <!-- Placeholder for gaze cursor -->
                <Canvas Width="200"
                        Height="200"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>

            </Grid>

        </Border>
        <controls:ScrollPageControl Grid.Column="2"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TargetScrollViewer="{Binding ElementName=Scroll}"/>

    </Grid>
</UserControl>
