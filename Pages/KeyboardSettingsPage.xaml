<UserControl x:Class="GazeStream.Pages.KeyboardSettingsPage"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="clr-namespace:GazeStream.Pages"
  xmlns:eye="clr-namespace:GazeStream.Eyetracker"
  xmlns:controls="clr-namespace:GazeStream.Controls"
  xmlns:settings="clr-namespace:GazeStream.AppData"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  mc:Ignorable="d"
  d:DataContext="{d:DesignInstance Type=settings:Settings}"
  d:DesignHeight="1080" d:DesignWidth="1920">

    <Grid Margin="24" Background="{StaticResource OptionsBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT PANEL: SETTINGS -->
        <ScrollViewer x:Name="Scroll">
            <StackPanel Grid.Column="0" VerticalAlignment="Top">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Margin" Value="0,10,0,0"/>
                    </Style>
                    <Style TargetType="{x:Type StackPanel}">
                        <Setter Property="Margin" Value="10,10,0,0"/>
                    </Style>
                </StackPanel.Resources>

                <TextBlock Text="Tiempo de activación" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <controls:EnumSelectorUniform SelectedValue="{Binding OpcionTiempoDeActivacionDeTeclado.Value}" Height="200"/>

                <TextBlock Text="Diseño del Teclado" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <controls:EnumSelectorUniform SelectedValue="{Binding KeyboardType.Value}" Height="200"/>

                <TextBlock Text="Tamaño del Teclado" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <controls:EnumSelectorUniform SelectedValue="{Binding KeyboardSize.Value}" Height="200"/>

                <TextBlock Text="Tamaño de fuente del Teclado" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <controls:EnumSelectorUniform SelectedValue="{Binding KeyboardFontSize.Value}" Height="200"/>

                <TextBlock Text="Retroalimentación de Teclado" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <eye:GazeButton HorizontalAlignment="Left" Width="300" Height="200" IgnoreRestMode="True">
                    <eye:GazeButton.Content>
                        <controls:IconToggleButton Content="Retroalimentación" IconSize="100" IconOn="/Resources/Icons/voice.png" IconOff="/Resources/Icons/voiceOff.png" IsChecked="{Binding VoiceFeedbackToggle.Value}"/>
                    </eye:GazeButton.Content>
                </eye:GazeButton>

                <TextBlock Text="Predicción de palabras" FontSize="25" Style="{StaticResource OptionTitle}"/>
                <StackPanel Orientation="Horizontal">
                    <eye:GazeButton HorizontalAlignment="Left" Width="300" Height="200" IgnoreRestMode="True">
                        <eye:GazeButton.Content>
                            <controls:IconToggleButton Content="Predicción" IconSize="100" IconOn="/Resources/Icons/voice.png" IconOff="/Resources/Icons/voiceOff.png" IsChecked="{Binding WordPrediction.Value}"/>
                        </eye:GazeButton.Content>
                    </eye:GazeButton>

                    <eye:GazeButton HorizontalAlignment="Left" Width="300" Height="200" IgnoreRestMode="True">
                        <eye:GazeButton.Content>
                            <controls:IconToggleButton Content="Aprendizaje de Palabras" IconSize="100" IconOn="/Resources/Icons/voice.png" IconOff="/Resources/Icons/voiceOff.png" IsChecked="{Binding WordPredictionLearning.Value}"/>
                        </eye:GazeButton.Content>
                    </eye:GazeButton>

                </StackPanel>

            </StackPanel>
        </ScrollViewer>

        <controls:ScrollPageControl Grid.Column="1"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TargetScrollViewer="{Binding ElementName=Scroll}"/>

    </Grid>
</UserControl>